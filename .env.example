# ==============================================================================
# AMBIENTE 1: DESARROLLO (npm run start:dev)
# ==============================================================================
# NODE_ENV="development"
# DATABASE_URL="postgresql://[user]:[password]@[neon_hostname]/[dbname]?sslmode=require"
# STORAGE_PROVIDER="r2"
# R2_ACCOUNT_ID="tu_account_id_de_cloudflare"
# R2_ACCESS_KEY_ID="tu_access_key"
# R2_SECRET_ACCESS_KEY="tu_secret_key"
# R2_BUCKET_NAME="msms-dev-bucket"
# CACHE_TYPE="memory"

# ==============================================================================
# AMBIENTE 2 & 3: PRE-PRODUCCIÓN Y PRODUCCIÓN (docker-compose)
# ==============================================================================
# NODE_ENV="preproduction" # Cambiar a "production" en el ambiente productivo

# -- Base de Datos (Docker) --
# DB_USER="user"
# DB_PASSWORD="password"
# DB_NAME="msms_db"
# DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?schema=public"

# -- Almacenamiento de Objetos (MinIO - Pre-Prod y Prod) --
# STORAGE_PROVIDER="minio"
# S3_ACCESS_KEY="minioadmin"
# S3_SECRET_KEY="minioadmin"
# S3_BUCKET_NAME="msms-bucket"
# NEXT_PUBLIC_STORAGE_URL="http://minio:9000/${S3_BUCKET_NAME}"

# -- Caché (Redis en Docker) --
# CACHE_TYPE="redis"
# REDIS_URL="redis://redis:6379"

# -- Puertos (opcional, estos son los defaults en docker-compose) --
# API_PORT=3000
# FRONTEND_PORT=3001
# DB_PORT=5432
# REDIS_PORT=6379
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001
# NGINX_HTTP_PORT=80
# NGINX_HTTPS_PORT=443
